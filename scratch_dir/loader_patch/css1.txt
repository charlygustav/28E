        /* ═══ LOADER ═══ */
        #loader {
            position: fixed !important;
            inset: 0 !important;
            background: #030304 !important;
            z-index: 200 !important;
            overflow: hidden !important;
            will-change: transform, opacity !important;
        }

        /* Perspective grid background */
        .loader-grid {
            position: absolute;
            inset: 0;
            background:
                linear-gradient(rgba(245,158,11,0.025) 1px, transparent 1px),
                linear-gradient(90deg, rgba(245,158,11,0.025) 1px, transparent 1px);
            background-size: 60px 60px;
            transform: perspective(400px) rotateX(55deg);
            transform-origin: center bottom;
            mask-image: radial-gradient(ellipse 100% 60% at 50% 100%, black 10%, transparent 70%);
            -webkit-mask-image: radial-gradient(ellipse 100% 60% at 50% 100%, black 10%, transparent 70%);
            pointer-events: none;
        }

        /* Scan lines overlay */
        .loader-scanlines {
            position: absolute;
            inset: 0;
            background: repeating-linear-gradient(
                0deg,
                transparent,
                transparent 2px,
                rgba(255,255,255,0.007) 2px,
                rgba(255,255,255,0.007) 4px
            );
            pointer-events: none;
            z-index: 5;
        }

        /* Floating particles container */
        #loader-particles {
            position: absolute;
            inset: 0;
            pointer-events: none;
            z-index: 3;
            overflow: hidden;
        }

        .loader-particle {
            position: absolute;
            border-radius: 50%;
            pointer-events: none;
            will-change: transform, opacity;
        }

        /* Ambient radial glow */
        .loader-ambient {
            position: absolute;
            top: 50%;
            left: 50%;
            transform: translate(-50%, -55%);
            width: 400px;
            height: 400px;
            background: radial-gradient(circle, rgba(245,158,11,0.06) 0%, rgba(236,72,153,0.03) 40%, transparent 70%);
            pointer-events: none;
            border-radius: 50%;
        }

        .loader-hidden {
            opacity: 0 !important;
            pointer-events: none;
            transition: opacity 0.5s ease;
        }
