        /* ═══ LOADER — CINEMATIC MEMORY GLASS (V5) ═══ */
        #loader {
            position: fixed !important;
            inset: 0 !important;
            background: radial-gradient(circle at center, #18181b 0%, #09090b 100%) !important;
            z-index: 200 !important;
            overflow: hidden !important;
            user-select: none;
            perspective: 1200px;
        }

        /* 3D Scene Container */
        #loader-scene-v5 {
            width: 100%;
            height: 100%;
            transform-style: preserve-3d;
            will-change: transform;
            display: flex;
            align-items: center;
            justify-content: center;
        }

        /* Ambient Center Glow */
        .loader-ambient-v5 {
            position: absolute;
            top: 50%;
            left: 50%;
            transform: translate(-50%, -50%);
            width: 50vw;
            height: 50vw;
            background: radial-gradient(circle, rgba(245,158,11,0.05) 0%, rgba(236,72,153,0.02) 40%, transparent 70%);
            pointer-events: none;
            z-index: 0;
        }

        /* Falling Embers / Petals */
        #loader-embers-v5 {
            position: absolute;
            inset: -100px;
            pointer-events: none;
            z-index: 1;
            transform: translateZ(-50px);
        }

        .ember-v5 {
            position: absolute;
            border-radius: 50%;
            background: #f59e0b;
            box-shadow: 0 0 10px rgba(245,158,11,0.8), 0 0 20px rgba(236,72,153,0.4);
            opacity: 0;
            will-change: transform, opacity;
        }

        /* Cinematic HUD Panel */
        .glass-panel-v5 {
            position: absolute;
            background: rgba(24, 24, 27, 0.4);
            backdrop-filter: blur(12px);
            -webkit-backdrop-filter: blur(12px);
            border: 1px solid rgba(255, 255, 255, 0.05);
            border-top: 1px solid rgba(255, 255, 255, 0.15);
            border-radius: 1rem;
            padding: 1.5rem;
            z-index: 10;
            box-shadow: 0 20px 40px -10px rgba(0,0,0,0.5);
            color: #fff;
            opacity: 0; /* GSAP will reveal */
            will-change: transform, opacity;
        }

        /* Header for HUD */
        .g-header {
            font-family: 'Inter', sans-serif;
            font-size: 0.65rem;
            text-transform: uppercase;
            letter-spacing: 0.2em;
            color: rgba(255,255,255,0.4);
            font-weight: 500;
            margin-bottom: 1rem;
            display: flex;
            align-items: center;
            gap: 8px;
        }

        .g-header::after {
            content: '';
            flex: 1;
            height: 1px;
            background: linear-gradient(90deg, rgba(255,255,255,0.1), transparent);
        }

        .loader-hidden {
            opacity: 0 !important;
            pointer-events: none;
            transition: opacity 0.8s ease;
        }
