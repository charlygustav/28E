            // V5 Elements (unique names to prevent collisions)
            const pctV5 = document.getElementById('loader-percent-v5');
            const progV5 = document.getElementById('loader-progress-v5');
            const sceneV5 = document.getElementById('loader-scene-v5');

            // ── GSAP Entrance & Interactions — Cinematic Memory Glass ──
            if (typeof gsap !== 'undefined') {
                const entranceTl = gsap.timeline({ defaults: { ease: 'power3.out', force3D: true } });

                // Init states
                gsap.set('.glass-panel-v5', { opacity: 0, scale: 0.95, filter: 'blur(10px)' });
                gsap.set('.hud-tl', { x: -30, y: -20 });
                gsap.set('.hud-tr', { x: 30, y: -20 });
                gsap.set('.hud-bl', { x: -30, y: 20 });
                gsap.set('.hud-br', { x: 30, y: 20 });
                gsap.set('#loader-core-v5', { opacity: 0, scale: 0.8 });
                gsap.set('.c-char', { opacity: 0, scale: 1.5, filter: 'blur(10px)' });
                
                // Entrance animation
                entranceTl
                    .to('#loader-core-v5', { opacity: 1, scale: 1, duration: 2, ease: 'power2.out' }, 0)
                    .to('.c-char', { opacity: 1, scale: 1, filter: 'blur(0px)', stagger: 0.15, duration: 1.5, ease: 'power3.out' }, 0.5)
                    .to('.glass-panel-v5', { opacity: 1, scale: 1, x: 0, y: 0, filter: 'blur(0px)', duration: 1.5, stagger: 0.1 }, 1.0);

                // Embers Particle System
                const eContainer = document.getElementById('loader-embers-v5');
                if (eContainer && !window.__PERF_LITE__) {
                    for (let i=0; i<40; i++) {
                        const p = document.createElement('div');
                        p.className = 'ember-v5';
                        const size = gsap.utils.random(1, 3);
                        p.style.width = size+'px'; p.style.height = size+'px';
                        p.style.left = gsap.utils.random(0, 100)+'%';
                        p.style.top = gsap.utils.random(80, 120)+'%'; // Start below
                        eContainer.appendChild(p);
                        
                        gsap.to(p, {
                            y: gsap.utils.random(-200, -800),
                            x: gsap.utils.random(-100, 100),
                            opacity: gsap.utils.random(0.2, 0.8),
                            duration: gsap.utils.random(8, 20),
                            repeat: -1,
                            delay: gsap.utils.random(0, 5),
                            ease: 'none'
                        });
                        
                        // Flicker
                        gsap.to(p, {
                            opacity: 0,
                            duration: gsap.utils.random(0.5, 2),
                            repeat: -1,
                            yoyo: true,
                            ease: 'sine.inOut'
                        });
                    }
                }
            }
